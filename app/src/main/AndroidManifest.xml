<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ypf.myuiapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyUiApplication">
        <activity android:name=".activity.MyRecyclerViewRecycleActivity" />
        <activity android:name=".activity.MyRecycleViewActivity" />
        <activity android:name=".activity.MyFishDrawableActivity" />
        <activity android:name=".activity.MySimpleColorChangeTextViewActivity" />
        <activity android:name=".activity.MyFlowLayoutActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>